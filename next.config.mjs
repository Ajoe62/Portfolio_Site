/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Fast Refresh is enabled by default in development
  // No need for explicit webpack configuration for hot reload
};

export default nextConfig;
